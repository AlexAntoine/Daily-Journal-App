<%- include('partials/header');%>
<h1>Home</h1>

<p> <%= _homeContent %> </p>

<% posts.forEach(function(post) { %>

   <h1> <%= post._title; %> </h1> 
   <p> <%= post.content.slice(0,100) + "..." %> 
      <a href= "posts/<%=  post._title; %>"">Read more...</a>
   </p>

<% }); %>

<%- include('partials/footer');%>